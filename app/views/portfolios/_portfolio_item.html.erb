

    
    <!-- example -->
    <div class="card">
    
        <span><p class="card-text"><%= link_to portfolio_item.title, portfolio_show_path(portfolio_item.id) %></p></span>
    <p class="card-text"><%= portfolio_item.subtitle %></p>

    <%= image_tag portfolio_item.thumb_image if !portfolio_item.thumb_image.nil? %>

 
    </div>